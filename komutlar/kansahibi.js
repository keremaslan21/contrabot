module.exports = {
  kod: "kansahibi",
  async run (client, message, args) {
    const { MessageEmbed } = require('discord.js')
      const embed = new  MessageEmbed()
      .setTitle('pOp piYasSI biTmİŞ lEğn kOrkUlaRı eNDer haTta reYNmen')
      message.channel.send(embed)
    }
 }
